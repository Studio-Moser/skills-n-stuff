server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    add_header X-Agent-Preview-Name "${PREVIEW_SERVICE_NAME}" always;

    location / {
        try_files $uri $uri/ =404;
    }
}
